import 'package:flutter/material.dart';

class AppColor{
  static const Color primary = Color.fromRGBO(26, 102, 66, 1);
  static const Color textTitle = Color.fromRGBO(9, 37, 24, 100);
  static const Color textHeader = Colors.white;
  static const Color textBody = Colors.black;
}